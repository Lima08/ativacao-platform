import LoginForm from 'components/LoginForm'

export default function Homepage() {
  return (
    <section className="container h-screen w-screen mx-auto flex  items-center justify-center">
      <LoginForm />
    </section>
  )
}
